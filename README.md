# Blockchain scheme written in go.  
#### Note:  
##### Writing JSON in Go transforms byte array data into Base64, so JSON blocks are'nt very readable but Base64 is not a cipher, thus it can be interpreted as a plain text
